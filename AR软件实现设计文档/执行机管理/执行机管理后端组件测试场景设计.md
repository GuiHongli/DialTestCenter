# 执行机管理后端组件测试场景设计

> **文档版本**：V4
> **最后更新**：2025-11-14
> **关联文档**：[执行机管理后端组件测试实现设计.md](./执行机管理后端组件测试实现设计.md)
> 
> **主要变更**：
> - ✅ 适配V4通信协议（JSON Text + Binary分片混合模式）
> - ✅ 重构文件传输测试（信令与分片分离）
> - ✅ 新增异步回调机制测试
> - ✅ 新增消息优先级队列测试

## 1. 测试概述

### 1.1 测试目标

验证执行机管理组件的核心功能，确保V4协议（JSON + Binary混合模式）在认证、状态管理、任务调度、文件传输等场景下的正确性和可靠性。

### 1.2 测试范围

| 测试类别 | 测试场景 | 优先级 |
|---------|---------|--------|
| **认证与会话** | Agent注册认证（IT-01）<br>Agent注销（IT-08） | P0 |
| **状态管理** | 心跳维护（IT-02）<br>离线重连（IT-04） | P0 |
| **任务管理** | 任务下发执行（IT-03）<br>任务停止（IT-09） | P0 |
| **UE管理** | App列表查询（IT-05a）<br>App安装（IT-05b）<br>截屏获取（IT-05c） | P1 |
| **脚本管理** | 脚本更新（IT-06） | P1 |
| **北向接口** | REST API（IT-07） | P1 |
| **V4特性** | 消息优先级队列（IT-10）<br>文件完整性（IT-11） | P0 |

### 1.3 测试策略

**集成测试策略**：
- 端到端验证业务流程（Agent ↔ 服务端）
- 基于Python + websocket-client实现Agent模拟器
- JSON消息格式验证 + Binary分片传输验证
- 数据库状态一致性验证
- 异步回调机制验证

---

## 2. 核心测试场景

### 2.1 Agent注册认证流程（IT-01）

**测试目标**：验证四阶段CHAP认证，适配V4的Base64/Hex编码

**前置条件**：
- dial_users表存在测试用户（username、NTLM Hash）
- executor表存在测试执行机记录

**测试步骤**：
1. 建立WebSocket连接
2. 发送Register-Request（JSON Text，含hostname）
3. 接收Register-Challenge（JSON Text，challenge为Base64编码）
4. 计算MD5(NTLM_Hash + Challenge)，编码为Hex
5. 发送Register-Response（JSON Text，含username、response）
6. 接收Register-Result（JSON Text，含result、token）

**验证点**：
- ✅ JSON信封结构正确（type、payload）
- ✅ Base64解码challenge成功，得到16字节随机数
- ✅ Hex编码response正确
- ✅ result=0表示成功，token为8字节整数
- ✅ 数据库：executor.status=1, executor.token已设置
- ✅ 会话绑定建立

**错误场景**：
- 错误的username → result≠0，描述包含错误原因
- 错误的response → 认证失败
- 重复连接同hostname → 旧连接断开，新连接认证

---

### 2.2 心跳状态维护流程（IT-02）

**测试目标**：验证JSON心跳机制和UE信息批量更新

**测试步骤**：
1. 基于IT-01完成认证
2. 构造Report-Msg（JSON Text，含token、state="Normal"、ue-list数组）
3. 接收Report-Ack（JSON Text）
4. 验证数据库更新
5. 每30秒重复一次

**验证点**：
- ✅ token在信封和payload中均存在
- ✅ ue-list为JSON数组，包含msisdn、serial、vendor、model、os-version
- ✅ Report-Ack的state=0表示成功
- ✅ 数据库：executor.last_online_time更新
- ✅ 数据库：ue表批量插入/更新，info字段存储JSON

**错误场景**：
- 无效token → 连接断开或返回错误
- 心跳停止90秒 → executor.status变为0（OFFLINE）

---

### 2.3 任务下发执行流程（IT-03）⚠️ 核心测试

**测试目标**：验证JSON信令 + Binary分片混合模式

**测试步骤**：
1. 服务端下发TaskStart-Request（JSON Text，含taskid、script-name等）
2. Agent模拟执行任务
3. **第一步**：Agent发送TaskStart-Response（JSON Text，含result、sub-result、filelen、crc，**不含文件**）
4. **第二步**：Agent连续发送N个Binary Message（日志文件分片，纯二进制）
5. 服务端InboundFileHandler接收所有分片
6. 服务端校验CRC32
7. 业务层通过onInboundFileComplete回调处理文件

**验证点**：
- ✅ JSON信令先于Binary分片到达
- ✅ Binary Message为纯二进制流（无JSON信封）
- ✅ 所有分片总大小 = JSON中的filelen
- ✅ CRC32校验通过
- ✅ InboundFileCompleteCallback被正确调用
- ✅ 临时文件成功存储，生成fileUrl
- ✅ 上层任务管理模块收到通知（含sub-result和fileUrl）

**错误场景**：
- 分片不完整（总大小 < filelen） → 超时错误
- CRC校验失败 → 业务层收到错误通知
- JSON信令后未发送Binary → 超时错误

---

### 2.4 离线处理和重连流程（IT-04）

**测试目标**：验证连接断开、状态清理和重连恢复

**测试步骤**：
1. **场景A：主动断开** - 关闭WebSocket连接，验证状态立即更新
2. **场景B：心跳超时** - 停止心跳90秒，验证超时检测
3. 验证离线状态：executor.status=0，会话绑定清理，token失效
4. 重新建立连接，尝试使用旧token（应失败）
5. 重新执行四阶段认证，获得新token

**验证点**：
- ✅ 连接断开后status立即更新为0
- ✅ 心跳超时90秒后status更新为0
- ✅ 会话绑定及时清理
- ✅ 旧token失效，无法使用
- ✅ 重连必须重新认证，不能复用旧token
- ✅ 新token与旧token不同

---

### 2.5 UE应用管理流程（IT-05）

#### IT-05a: App列表查询

**测试步骤**：
1. 发送AppList-Query（JSON Text，含serial-no）
2. 接收AppList-Response（JSON Text，含app-list数组）

**验证点**：
- ✅ app-list数组解析正确（package、name、version）
- ✅ state=0表示成功

#### IT-05b: App安装

**测试步骤**：
1. 服务端发送AppInstall-Request（JSON Text，含filelen、filetype、crc）
2. 服务端连续发送N个Binary Message（APK或脚本分片）
3. Agent接收、校验CRC、执行安装
4. Agent发送AppInstall-Response（JSON Text，含state）

**验证点**：
- ✅ 支持filetype="package"（APK直装）和"script"（脚本安装）
- ✅ Binary分片传输正确
- ✅ CRC校验通过
- ✅ state=0表示安装成功

#### IT-05c: 截屏获取

**测试步骤**：
1. 服务端发送ScreanCap-Query（JSON Text）
2. Agent执行ADB截屏
3. Agent发送ScreanCap-Response（JSON Text，含filename、filelen、crc）
4. Agent连续发送N个Binary Message（PNG图片分片）
5. 服务端接收、校验、存储

**验证点**：
- ✅ Agent→服务端方向的文件传输正确
- ✅ PNG文件格式有效
- ✅ 异步回调正确

---

### 2.6 脚本更新流程（IT-06）

**测试步骤**：
1. 服务端发送ScriptUpdate-Notify（JSON Text，含script-name、version、filelen、crc）
2. 服务端连续发送N个Binary Message（脚本文件分片）
3. Agent接收、校验CRC、保存脚本
4. Agent发送ScriptUpdate-Ack（JSON Text，含state）

**验证点**：
- ✅ JSON信令先于Binary分片
- ✅ CRC校验通过
- ✅ state=0表示更新成功
- ✅ 版本管理正确

---

### 2.7 北向API测试（IT-07）

**测试步骤**：
1. GET /api/executors - 查询执行机列表
2. POST /api/executors/refresh - 刷新执行机信息

**验证点**：
- ✅ HTTP 200状态码
- ✅ JSON响应包含执行机信息和UE数量
- ✅ 敏感信息过滤（不返回token）
- ✅ 刷新操作触发WebSocket消息

---

### 2.8 Agent注销流程（IT-08）

**测试步骤**：
1. 发送DeRegister-Request（JSON Text，含hostname）
2. 接收DeRegister-Ack（JSON Text）
3. 验证status=0，会话清理

**验证点**：
- ✅ result=0表示注销成功
- ✅ 资源清理完整
- ✅ 可重新注册

---

### 2.9 任务停止流程（IT-09）

**测试步骤**：
1. 基于IT-03启动任务
2. 服务端发送TaskStop-Request（JSON Text）
3. Agent立即终止任务进程
4. Agent发送TaskStop-Response（JSON Text）

**验证点**：
- ✅ 任务进程被立即终止
- ✅ state=0表示停止成功
- ✅ 上层收到停止通知

---

### 2.10 消息优先级队列测试（IT-10）⚠️ V4核心

**测试目标**：验证高优JSON信令能插队低优Binary分片

**测试步骤**：
1. 服务端开始下发10MB脚本（约100个分片）
2. 发送到第10个分片时，插入TaskStop-Request
3. 验证TaskStop立即发送（插队）
4. 验证Agent先收到TaskStop
5. 验证脚本分片继续传输

**验证点**：
- ✅ TaskStop插队成功，不等待分片完成
- ✅ 高优消息延迟 < 100ms
- ✅ 插队不影响分片完整性
- ✅ 不引入死锁或阻塞

---

### 2.11 文件分片完整性测试（IT-11）

**测试目标**：验证文件传输异常处理

**测试步骤**：
1. **场景A**：分片不完整 - 声明1MB，只发送500KB → 验证超时检测
2. **场景B**：CRC错误 - 故意修改分片内容 → 验证CRC校验失败
3. **场景C**：分片顺序错乱 → 验证错误处理

**验证点**：
- ✅ 超时机制正确（建议60秒）
- ✅ CRC校验正确
- ✅ 临时文件被清理
- ✅ 业务层收到错误通知

---

## 3. 验证清单

### 3.1 功能验证

| 功能模块 | 验证项 | 状态 |
|---------|-------|-----|
| 认证与会话 | 四阶段CHAP认证 | ☐ |
| | Base64/Hex编码 | ☐ |
| | Token会话管理 | ☐ |
| 状态管理 | JSON心跳机制 | ☐ |
| | UE信息批量更新 | ☐ |
| | 离线检测与重连 | ☐ |
| 任务管理 | JSON信令 + Binary分片 | ☐ |
| | 异步文件回调 | ☐ |
| | 任务停止 | ☐ |
| UE管理 | App列表查询 | ☐ |
| | App安装（两种模式） | ☐ |
| | 截屏获取 | ☐ |
| 脚本管理 | 脚本更新与版本管理 | ☐ |
| V4特性 | 消息优先级队列 | ☐ |
| | 文件完整性检测 | ☐ |

### 3.2 技术验证

| 技术项 | 验证内容 | 标准 |
|-------|---------|-----|
| JSON协议 | 所有消息类型（0x01-0x36） | 格式正确 |
| 编解码 | Base64（challenge） | 正确编解码 |
| | Hex（response、crc） | 正确编解码 |
| 文件传输 | JSON信令先于Binary分片 | 顺序正确 |
| | Binary为纯二进制流 | 无信封 |
| | CRC32校验 | 校验通过 |
| 异步机制 | InboundFileCompleteCallback | 正确调用 |
| 队列机制 | 高/低优先级队列 | 插队正确 |
| 数据库 | executor、ue、dial_users表 | 状态一致 |

### 3.3 性能验证

| 性能指标 | 目标值 | 验证方法 |
|---------|-------|---------|
| 认证流程耗时 | < 2秒 | 计时测量 |
| 心跳响应延迟 | < 500ms | 计时测量 |
| 任务下发延迟 | < 1秒 | 计时测量 |
| 高优消息插队延迟 | < 100ms | 计时测量 |
| 文件传输吞吐量 | > 1MB/s | 传输速率测量 |
| 数据库查询响应 | < 100ms | 计时测量 |
| 并发连接支持 | ≥ 100个执行机 | 压力测试 |

### 3.4 安全验证

| 安全项 | 验证内容 | 状态 |
|-------|---------|-----|
| 密码安全 | NTLM Hash存储，不暴露明文 | ☐ |
| 会话安全 | Token失效机制 | ☐ |
| 数据安全 | 北向API不泄露token | ☐ |
| 传输安全 | WSS TLS加密 | ☐ |
| 认证安全 | 失败正确拒绝 | ☐ |

---

## 4. 测试执行策略

### 4.1 测试优先级

**P0（必须通过）**：
- IT-01, IT-02, IT-03, IT-04, IT-10, IT-11

**P1（重要功能）**：
- IT-05a/b/c, IT-06, IT-07

**P2（辅助功能）**：
- IT-08, IT-09

### 4.2 测试依赖关系

```
IT-01 (认证) 
  ├─→ IT-02 (心跳)
  ├─→ IT-03 (任务) ─→ IT-09 (停止)
  ├─→ IT-04 (离线)
  ├─→ IT-05 (UE管理)
  ├─→ IT-06 (脚本)
  ├─→ IT-08 (注销)
  ├─→ IT-10 (队列)
  └─→ IT-11 (完整性)

IT-07 (API) - 独立执行
```

### 4.3 回归测试策略

**代码变更后必须执行**：
- 通信层修改 → P0全量测试
- 业务层修改 → 相关场景测试
- 数据库变更 → P0 + P1测试

**发布前检查**：
- 所有P0和P1测试通过
- 性能指标达标
- 安全验证通过

---

## 5. 测试数据要求

### 5.1 数据库测试数据

**dial_users表**：
- 至少1个测试用户（username: test-user, password: NTLM Hash）

**executor表**：
- 至少2个测试执行机记录（用于并发测试）

**ue表**：
- 初始为空，由测试动态创建

### 5.2 文件测试数据

**脚本文件**：
- 小文件：< 1MB（快速测试）
- 大文件：5-10MB（性能测试）

**APK文件**：
- 标准APK：2-5MB
- 大APK：> 20MB（可选）

**截图文件**：
- PNG格式，分辨率1080x1920

---

**文档版本：** V4
**最后更新：** 2025-11-14
**维护者：** 执行机管理开发团队

